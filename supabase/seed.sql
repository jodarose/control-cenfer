-- Seed data — applied manually to Cloud via SQL editor when needed.
-- Auth users must be created via Supabase Auth (dashboard or signup), then
-- their profile row is inserted matching the auth.users.id.

-- Example (replace UUID with a real auth.users.id):
-- insert into profiles (id, nombre, apellido, rol)
-- values ('00000000-0000-0000-0000-000000000000', 'Admin', 'Cenfer', 'super_admin');
